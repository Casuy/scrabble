this is a socket version of the system
many errors have not been handled and the system is incomplete before we decide to implement the system with RMI
we choose TCP socket and implement the inital version of the game
the system includes multi-thread, Swing GUI and communication between server and client
which can support a simple multiplayer game